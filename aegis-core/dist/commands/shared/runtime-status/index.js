import execute from './execute.js';
export default {
    name: 'runtime-status',
    description: 'Inspect AEGIS stabilized runtime status, active session, checkpoint, health, and lock state.',
    execute
};
